# Velorak landing media

Drop real assets here and wire them into the placeholder slots:

- `hero-dashboard.png` — main hero product shot
- `dashboard.png`, `tasks.png`, `cost.png`, `reports.png` — screenshot gallery
- product video / embed — replace `VideoSlot` placeholder

Placeholder UI currently lives in the React components; swap images when ready.
